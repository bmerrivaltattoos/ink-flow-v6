{
  "name": "ink-flow",
  "version": "6.0.0-beta.1",
  "private": true,
  "description": "Server-rendered tattoo booking with private Supabase storage",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
